# ES6综合练习之飞机大战



本次游戏采用HTML的网页展现形式，使用HTML5里面canvas画布进行，

​	使用canvas画布来绘制图像
​    创建画布元素
​    获取画布上下文：
​    绘制图像：
​    绘制图像文件：
游戏采用面向对象的方法开发 并分成多个js文件是结构更加清晰
使用assetsConfig.js文件来管理 资源的加载

创建游戏的背景对象  创建玩家飞机对象  将其画在画布上  利用setInterval方法来隔一段时间重绘图片并改变 飞机 背景 子弹 敌机的位置 来实现动画效果

设置了一个访问器属性 level ，然后通过这个访问器属性来控制maxEnemyCount 的值，以决定敌机的数量

通过反射拿到所有的属性名，然后遍历 


我们新建了一个 GameObject 的一公共对象，将之前的代码提取之来了 来减少一些重复的代码

当敌机与子弹相撞的时候 显示 爆炸图片

1. 在 assetsConfig.js 文件里面，找到 assetsList ，添加需要加载的玩家飞机
   图片

2. 在js目录下面创建 Hero.js
3. 在创建的JS文件里面，编写对象代码
4. 在index.js的文件里面，找到 gameContainer ，添加一个属性名为 hero
5. 在index.js的文件里在，找到 init() 方法，加载资源完成以后，创建new
Hero();
6. 在index.js文件里在，找到 draw() 方法，绘制玩家飞机


### **二、创建游戏控制对象**

采用面向对象的方式进行开发

戏